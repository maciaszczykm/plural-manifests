apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: test-helm-hooks
spec:
  namespace: test-helm-hooks
  git:
    folder: service-test-helm-hooks
    ref: main
  repositoryRef:
    kind: GitRepository
    name: plural-manifests
  clusterRef:
    kind: Cluster
    name: target-cluster
    namespace: default