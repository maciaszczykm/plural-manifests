apiVersion: deployments.plural.sh/v1alpha1
kind: ServiceDeployment
metadata:
  name: test-sync-hooks
spec:
  namespace: test-sync-hooks
  git:
    folder: service-test-sync-hooks
    ref: main
  repositoryRef:
    kind: GitRepository
    name: plural-manifests
  clusterRef:
    kind: Cluster
    name: target-cluster
    namespace: default