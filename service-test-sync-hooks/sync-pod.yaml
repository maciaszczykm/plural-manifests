apiVersion: v1
kind: Pod
metadata:
  name: sync-pod
  annotations:
    plural.sh/sync-wave: "6"
    deployment.plural.sh/sync-hook: "sync"
spec:
  containers:
    - name: hello-world
      image: alpine:latest
      command: ["/bin/sh", "-c"]
      args:
        - while true; do echo "Hello World"; sleep 30; done
